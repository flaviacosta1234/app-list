import React from "react";

const SearchApp = () => {
  return (
    <div>
      <header>
        <input type="text" placeholder="Search by App" />
      </header>
    </div>
  );
};

export default SearchApp;
